<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>fr.damnardev.example</groupId>
	<artifactId>pitest-example-wrapper</artifactId>
	<version>1.0.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>Pitest Example Wrapper</name>

	<licenses>
		<license>
			<name>Apache-2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<developers>
		<developer>
			<id>damnardev</id>
			<name>Damien</name>
			<email>admin@damnardev.fr</email>
		</developer>
	</developers>

	<scm>
		<url>https://github.com/damnardev-projects/pitest-example</url>
		<connection>scm:git:git://github.com/damnardev-projects/pitest-example.git</connection>
		<developerConnection>scm:git:git@github.com:damnardev-projects/pitest-example.git</developerConnection>
		<tag>HEAD</tag>
	</scm>

	<properties>
		<jacoco.maven.plugin.version>0.8.15</jacoco.maven.plugin.version>
		<maven.enforcer.plugin.version>3.6.3</maven.enforcer.plugin.version>
		<sonar.maven.plugin.version>5.8.0.7211</sonar.maven.plugin.version>
		<versions.maven.plugin.version>2.21.0</versions.maven.plugin.version>

		<sonar.projectKey>pitest-example</sonar.projectKey>
		<sonar.projectName>Pitest Example</sonar.projectName>
		<sonar.coverage.jacoco.xmlReportPaths>
			${maven.multiModuleProjectDirectory}/pitest-example-tests/target/site/jacoco-aggregate/jacoco.xml
		</sonar.coverage.jacoco.xmlReportPaths>
		<jacoco.excludes>**/generated/**,**/target/**</jacoco.excludes>
	</properties>

	<modules>
		<module>pitest-example-dependencies</module>
		<module>pitest-example-parent</module>
		<module>pitest-example-internal-parent</module>
		<module>pitest-example-module-A</module>
		<module>pitest-example-module-B</module>
		<module>pitest-example-tests</module>
	</modules>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>${jacoco.maven.plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-enforcer-plugin</artifactId>
					<version>${maven.enforcer.plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.sonarsource.scanner.maven</groupId>
					<artifactId>sonar-maven-plugin</artifactId>
					<version>${sonar.maven.plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>versions-maven-plugin</artifactId>
					<version>${versions.maven.plugin.version}</version>
					<configuration>
						<ignoredVersions>.*[-.]M\d+,.*-SNAPSHOT,.*[-.]alpha.*,.*[-.]beta.*</ignoredVersions>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<executions>
					<execution>
						<id>enforce-maven</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>[3.9,)</version>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.sonarsource.scanner.maven</groupId>
				<artifactId>sonar-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
